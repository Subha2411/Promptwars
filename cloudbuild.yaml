steps:
  # Build Docker image (multi-stage: flutter build + nginx serve)
  - name: 'gcr.io/cloud-builders/docker'
    args:
      - 'build'
      - '-t'
      - 'gcr.io/$PROJECT_ID/arena-iq:$COMMIT_SHA'
      - '-f'
      - 'arena_iq/Dockerfile'
      - 'arena_iq'
    timeout: '1200s'

  # Push image to Container Registry
  - name: 'gcr.io/cloud-builders/docker'
    args:
      - 'push'
      - 'gcr.io/$PROJECT_ID/arena-iq:$COMMIT_SHA'

  # Deploy to Cloud Run
  - name: 'gcr.io/google.com/cloudsdktool/cloud-sdk'
    entrypoint: gcloud
    args:
      - 'run'
      - 'deploy'
      - 'arena-iq'
      - '--image'
      - 'gcr.io/$PROJECT_ID/arena-iq:$COMMIT_SHA'
      - '--region'
      - 'asia-south1'
      - '--platform'
      - 'managed'
      - '--allow-unauthenticated'
      - '--port'
      - '8080'

images:
  - 'gcr.io/$PROJECT_ID/arena-iq:$COMMIT_SHA'

timeout: '1800s'
